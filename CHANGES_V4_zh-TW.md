# PAWPAY v4：兩張不同的立體棋盤

## 修改內容

台灣使用較大的海岸輪廓與島內環形道路，房屋改為小型粉彩屋頂、奶油色牆面與圓形地基；中央配置低山、湖泊、田地、樹木與風車。所有 28 格與各種等級模型都經幾何檢查，沒有落在海上。

天空島改成六座高度與形狀不同的浮島，22 格分配在各島陸地上；拱形木橋連接走格路徑，中央獨立城堡島有彩虹與晶石。建築使用圓塔與尖屋頂，浮島下方有岩石底部與雲層。

地形與道路從同一份產生器輸出，地點索引沿用原順序。地名分列，避免標籤相互覆蓋；縮放後畫面外的名稱仍可從全部地點查看。鏡頭可旋轉、縮放、回正。改動僅在呈現層，不改 PlaygroundPage 的原始規則或地名資料。

## 固定收入

最新截圖已定位為缺少確認 API／錯誤路由的 HTTP 404。伺服器必須套用 backend_update 並重啟，無法透過重新安裝 Flutter App 完成伺服器更新。

本次保留已具備確認、取消與復原的後端模組，新增互動式安裝工具與 Windows 啟動檔，先做相容性檢查再備份套用；新增具體的內外網健康檢查、404 排解與交接說明。診斷工具的月份改為執行當月。

未更改 App 連線 IP、5000／8000 埠、登入驗證、固定收支規則或資料庫憑證。沒有以本機假資料隱藏錯誤。

## 保留範圍

與 v3 逐檔比對：PlaygroundPage、GachaPage、扭蛋模型與繪製元件、城市元件、固定收入的記帳／審核核心、pubspec.yaml、pubspec.lock、Android 原始設定不變。除了地圖呈現、相關測試、後端安裝診斷工具與交付文件，其餘原始檔不變。

## 已執行

- Dart 3.10.4 的語法解析／格式化與本機 import 檢查。
- 兩張模型的全部頂點數值、各格所有建築樣式的陸地包含檢查、所有圓形地基間距檢查。
- 相同模型的獨立渲染檢查，包含預設視角、旋轉視角與小尺寸。
- 28 項 Python 後端／診斷／互動式安裝離線測試。
- 增補安裝工具套用於配套後端的暫存副本，檢查原有函式未變、備份與重複套用。
- ZIP 內容與 SHA-256 完整性檢查。

## 尚未驗證

嘗試安裝同版本 Flutter 3.38.5 進行完整分析／測試時，工具流程因存取非專案所需的雲端環境資料而被自動審核拒絕，未繼續該流程。因此未完成 Flutter analyze、Flutter test、APK、手機效能或線上 MySQL 驗證。附上的 Flutter 測試請在原本可執行的電腦依教學執行。

後端測試透過 SQLite 相容轉接層與測試路由執行，不能取代真實 MySQL 行鎖、既有資料關聯與伺服器部署驗證。預覽不是 Flutter 或 Android 真機截圖。
